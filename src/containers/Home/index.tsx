import styled from 'styled-components';

export const Home: React.FC = () => {
  return <ScContainer>HOME</ScContainer>;
};

const ScContainer = styled.div`
  width: 100%;
  height: 100%;
  color: var(--primary);
`;
